spring:
  jpa:
    show-sql: false
  flyway:
    clean-disabled: true

logging:
  level:
    root: WARN
    com.thegamersstation.marketplace: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

otp:
  provider: simulated
messaging:
  ably:
    enabled: true

# Use local storage as fallback in production when S3 fails
#media:
#  storage:
#    local:
#      upload-dir: uploads
#      base-url: http://91.98.156.117/api/v1/uploads
